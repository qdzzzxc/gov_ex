# Задача 4. Очередь.

## Условие
Реализуйте структуру данных «Очередь» на языке программирования по вашему выбору. Необходимо реализовать создание пустой очереди, добавление элемента очереди, удаление элемента, отображение всех элементов очереди. Желательно использовать ООП.

## Решение

### Программа минимум

```python
from collections import deque
class Queue:
    def __init__(self): self.q = deque()       # пустая очередь
    def push(self, x): self.q.append(x)        # добавить в хвост
    def pop(self): return self.q.popleft()     # удалить из головы
    def show(self): return list(self.q)        # вывести от головы к хвосту
```

### Полное решение

### Идея
FIFO: добавление в хвост, удаление из головы. Реализация на `collections.deque` даёт $O(1)$ для обоих концов (внутри — двусвязный список блоков).

### Код

```python
from collections import deque

class Queue:
    def __init__(self):
        self._data = deque()

    def enqueue(self, v): self._data.append(v)

    def dequeue(self):
        if not self._data: raise IndexError("dequeue from empty queue")
        return self._data.popleft()

    def peek(self):
        if not self._data: raise IndexError("peek from empty queue")
        return self._data[0]

    def __len__(self):  return len(self._data)
    def __iter__(self): return iter(self._data)  # фронт → хвост

    def __repr__(self):
        return "Queue(front→back)[" + ", ".join(map(repr, self)) + "]"
```

### Прогон
```
enqueue 1,2,3 → Queue(front→back)[1, 2, 3], len = 3
peek    = 1
dequeue = 1 → Queue(front→back)[2, 3]
```

### Сложности
- `enqueue`, `dequeue`, `peek`, `len` — $O(1)$.
- Итерация — $O(n)$.

### Замечания
- На обычном `list` `popleft` стоит $O(n)$ из-за сдвига — поэтому `deque`.
- Альтернатива — кольцевой буфер на массиве фиксированной длины (для bounded queue) или собственный связный список с `head`/`tail`.
